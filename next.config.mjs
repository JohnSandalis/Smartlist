/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.e-food.gr",
      "warply.s3.eu-west-1.amazonaws.com",
      "e-katanalotis.gov.gr",
    ],
  },
};

export default nextConfig;
